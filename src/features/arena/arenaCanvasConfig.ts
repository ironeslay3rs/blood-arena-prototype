/**
 * Phase-1 Canvas layer under the arena (see `docs/BLOOD_ARENA_MASTER_PLAN.md` §9).
 * Sprite atlases can target the same coordinate mapping later.
 */
export const ARENA_COMBAT_CANVAS_ENABLED = true;
